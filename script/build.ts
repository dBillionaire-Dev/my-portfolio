// Note: This build script was used for the old Vite + Express setup.
// Since migration to Next.js, the build is handled by Next.js directly.
// Run: npm run build (uses next build)

console.log("Build is now handled by Next.js");

