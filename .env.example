# Neon Database Connection (Required for Vercel deployment)
# Get these from your Neon dashboard: https://console.neon.tech
POSTGRES_URL=postgres://username:password@ep-xxx.region.neon.tech/portfolio?sslmode=require
POSTGRES_USER=username
POSTGRES_HOST=ep-xxx.region.neon.tech
POSTGRES_PASSWORD=your_password
POSTGRES_DATABASE=portfolio

# For local development, you can also use DATABASE_URL
# DATABASE_URL=postgres://username:password@localhost:5432/portfolio

# JWT Secret for authentication (generate a strong random string)
# Run: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-at-least-32-chars

# Optional: Next.js API URL for local development
# NEXT_PUBLIC_API_URL=http://localhost:3000

# Node environment
NODE_ENV=production

